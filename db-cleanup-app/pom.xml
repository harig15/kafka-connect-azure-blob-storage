<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.4</version>
        <relativePath/>
    </parent>

    <groupId>com.dbcleanup</groupId>
    <artifactId>db-cleanup-app</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>DB Cleanup Application</name>
    <description>Policy-driven database cleanup application with web dashboard</description>
    <packaging>jar</packaging>

    <properties>
        <java.version>17</java.version>
        <!-- Spring Cloud Contract 4.1.x is the release line compatible with Spring Boot 3.2.x -->
        <spring-cloud-contract.version>4.1.4</spring-cloud-contract.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Thymeleaf for server-side templates -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <!-- Spring Data MongoDB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-mongodb</artifactId>
        </dependency>

        <!-- Embedded MongoDB for PoC (no external MongoDB server required) -->
        <dependency>
            <groupId>de.flapdoodle.embed</groupId>
            <artifactId>de.flapdoodle.embed.mongo.spring30x</artifactId>
            <version>4.11.0</version>
        </dependency>

        <!-- Configuration properties support -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Lombok for reducing boilerplate -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
            Consumer-driven contract testing (producer side).
            Brings in the contract verifier runtime plus REST Assured MockMvc,
            which the generated contract tests are written against.
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-contract-verifier</artifactId>
            <version>${spring-cloud-contract.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!--
                Spring Cloud Contract - producer side.

                Wires two goals into the standard lifecycle:
                  generate-test-sources : generateTests  - turns every contract under
                                                           src/test/resources/contracts into a
                                                           JUnit 5 test that calls the real
                                                           controller through MockMvc.
                  package               : generateStubs  - packages the same contracts as
                                                           WireMock stub mappings into
                                                           db-cleanup-app-<version>-stubs.jar,
                                                           which consumers use to stand up a
                                                           fake of this API.

                The generated tests run in the normal `mvn test` phase via Surefire, so a
                change that breaks the published API fails the build.
            -->
            <plugin>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-contract-maven-plugin</artifactId>
                <version>${spring-cloud-contract.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <testFramework>JUNIT5</testFramework>
                    <testMode>MOCKMVC</testMode>
                    <baseClassForTests>com.dbcleanup.contract.CleanupApiBase</baseClassForTests>
                    <basePackageForTests>com.dbcleanup.contract.generated</basePackageForTests>
                </configuration>
            </plugin>

            <!--
                Keeps the generated contract tests in the test compilation unit.

                When the contracts have not changed, spring-cloud-contract skips generation
                ("Nothing to generate - all classes are up to date") and, in doing so, does not
                register target/generated-test-sources/contracts as a test source root. The
                compiler then rebuilds from src/test/java alone, clears target/test-classes, and
                Surefire finds no contract tests at all - a green build that verified nothing.
                Registering the directory unconditionally makes incremental builds behave like
                clean ones.
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>add-contract-test-sources</id>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <goal>add-test-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.build.directory}/generated-test-sources/contracts</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!--
                Second line of defence for the same failure mode: if the contract tests ever
                stop being discovered, fail the build instead of reporting success.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <failIfNoTests>true</failIfNoTests>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
